plugins {
  id 'com.moowork.node' version '1.2.0'
}

task clean(type: Delete) {
  group = 'build'
  delete 'dist'
}

task build {
  group = 'build'
  dependsOn "ngBuild"
}

task ngBuild(type: NpmTask) {
  args = ['run', 'build']
}
